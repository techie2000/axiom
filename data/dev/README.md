# Development Environment Data

This folder uses the shared guidance in `../README.md` to avoid duplicated documentation drift.

Dev-specific reminder:

- This environment is usually more disposable than `main`.
- Prefer non-destructive stops (`docker compose ... down`) during normal use.
- Use `down -v` only when you intentionally want a full reset.
