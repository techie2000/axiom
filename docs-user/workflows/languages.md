# Languages

## Goal

Browse and search ISO 639-1 language reference data in Axiom, including writing direction information.

## Prerequisites

- You are signed in to Axiom. See [Sign In & Access](../getting-started/sign-in-and-access).

## Steps

### Search for a language

1. Click **Languages** in the top navigation.
2. Type in the **Search** bar to filter by language name, native name, or ISO 639-1 code.
3. Results update automatically as you type.

### Filter by writing direction

1. On the Languages page, locate the filter bar below the stats cards.
2. Use the **Direction** dropdown to show only left-to-right (LTR) or right-to-left (RTL) languages.
3. Alternatively, click the **LTR** or **RTL** stat card to apply the same filter quickly.
4. Click **Clear Filters** to reset all active filters.

### Adjust page width

Click the **Expand / Normal** toggle in the page header to switch between a wider table view and
standard width. Your preference is saved automatically.

## Fields

| Field | Description | Example |
| --- | --- | --- |
| Code | ISO 639-1 language code | `en` |
| Name | Official English name of the language | `English` |
| Native Name | Language name written in that language | `English` |
| Direction | Writing direction of the language | `LTR` / `RTL` |

## Expected result

You can find any ISO 639-1 language by name or code, filter by writing direction, and view all
available language attributes.

## Common issues

| Issue | Possible cause | Resolution |
| --- | --- | --- |
| Language not found | Search term does not match name or code | Try the ISO 639-1 code directly (for example, `fr`) |
| RTL filter shows no results | No RTL languages match the current search term | Clear the search and apply the direction filter alone |
| Native name is blank | Not all records include a native name | This is expected for some languages |

## Related tasks

- [Countries](./countries) — country reference data including the language codes spoken per country.
- [Data Dictionary](../reference/data-dictionary) — definitions for all language fields.
- [Navigation Basics](../getting-started/navigation-basics) — adjust page width and use search and filter controls.
